# Huffman
Codificação de uma imagem por Huffman

# GraphQL query

`query{\n
    appointmentQuestionsHistory(deepth: 1){`
        `date`
        `daily{`
            `surveyName`
            `questionsBySurvey{`
                `name`
                `value`
            `}`
        `} `                                                                                                                 
    `}`
`}`

